django
djangorestframework
gunicorn
psycopg2-binary  # Para conectar PostgreSQL (lo usaremos más adelante)
psycopg2-binary
dj-database-url
djangorestframework-simplejwt
django-extensions
django-cors-headers
requests
python-dotenv  # 🔥 Para leer archivos .env automáticamente
Pillow  # Para ImageField en Django
redis  # Para cache y sesiones
django-redis  # Integración Django-Redis
celery  # Tareas asíncronas (migración HPS)
PyMuPDF  # Para rellenar PDFs automáticamente
channels>=4.0.0  # Django Channels para WebSocket
channels-redis>=4.1.0  # Redis channel layer para Channels
daphne>=4.0.0  # Servidor ASGI para Django Channels (WebSocket)
openai>=1.3.0  # Cliente OpenAI para agente IA
httpx>=0.24.0  # Cliente HTTP async para agente IA
